// Declare Two Integers and Calculate Sum: Declare two integer variables n1 and n2, then calculate the sum of them.
#include <stdio.h>
int main() {
    int one = 20, two = 30;

    printf("20 + 30 = %d\n", one + two);

    return 0;
}