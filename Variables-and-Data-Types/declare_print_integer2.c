// Declare and Print 2 Integers: Declare two integer variables, assign values 10 and 20 respectively. Print both integer value.
#include <stdio.h>
int main() {
    int x = 20, y = 30;
    
    printf("Both integer value are: %d and %d\n", x, y);
    
    return 0;
}