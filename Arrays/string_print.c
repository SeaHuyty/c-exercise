// Print a string stored in a character array.
// Declare a character array to store a string and print the string.

#include <stdio.h>

int main() {
    // Initialize a character array with a string
    char task_six[20] = "Hello, World!!";

    // Print the string
    printf("%s\n", task_six);

    return 0;
}
